#ПРИМЕР .env ФАЙЛА


# Порт gRPC сервера
# Формат: <номер_порта>
GRPC_PORT=50051

# Название сервиса(пока не используется, TBA)
SERVICE_NAME=order-service

# Тип среды(local, dev, prod)
ENV_TYPE=local